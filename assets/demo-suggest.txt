Auto Model Router demo — boundary-gated confirm

User: Rename the variable foo to bar in utils.py
Agent: Auto continues on **fast** — Clear, bounded, low-judgment task; lightest configured provider tier.
Agent: Auto-continued on fast. Running with the host's configured fast model/effort.

User: Review this auth change for XSS and credential leaks
Agent: Auto suggests **reasoning** — Ambiguous requirements, unknown-cause debug, architecture, or security-sensitive work. Confirm required (high-risk / hard to undo), or override (fast | standard | reasoning | max).
User: confirm
Agent: Confirmed reasoning. Running with the host's configured reasoning model/effort.

User: Rename the helper and apply the same null-check pattern across a few files
Agent: Auto suggests **standard** — Multi-file / known-pattern work beyond a trivial single edit. Confirm to run, or override (fast | standard | reasoning | max).
User: confirm
Agent: Confirmed standard. Running with the host's configured standard model/effort.
