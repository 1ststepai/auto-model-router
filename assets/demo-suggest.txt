Auto Model Router demo — boundary-gated confirm + Cursor picker action

User: Rename the variable foo to bar in utils.py
Agent: Auto continues on **fast** — Clear, bounded, low-judgment task; lightest configured provider tier. Switch Cursor picker to <your-fast-model> / low effort if the current model is heavier than needed.

User: Review this auth change for XSS and credential leaks
Agent: Auto suggests **reasoning** — Ambiguous requirements, unknown-cause debug, architecture, or security-sensitive work. Confirm required (high-risk / hard to undo), or override (fast | standard | reasoning | max). Switch Cursor picker to <your-reasoning-model> / high effort (current pick looks lighter than needed).
User: confirm
Agent: Confirmed reasoning. Waiting for the Cursor picker switch to the mapped reasoning model/effort, then running.

User: Rename the helper and apply the same null-check pattern across a few files
Agent: Auto suggests **standard** — Multi-file / known-pattern work beyond a trivial single edit. Confirm to run, or override (fast | standard | reasoning | max). Switch Cursor picker to <your-standard-model> / medium effort if the current model is heavier or lighter than needed.
User: confirm
Agent: Confirmed standard. Running after the mapped picker/effort is selected.
